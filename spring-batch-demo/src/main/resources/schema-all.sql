CREATE TABLE facebook  (
    id BIGINT AUTO_INCREMENT NOT NULL PRIMARY KEY,
    date date,
    media VARCHAR(50),
    namelpid varchar(100),
    cost float,
    impression int,
    click int,
    cv int
);

